centos:
  distribution: centos
  package_type: rpms
  version_examples: 7.7, 8.0

  upgrade:
    - dnf --help >/dev/null 2>&1 && DNF=dnf || DNF=yum
    - $DNF upgrade -y

  install:
    - $DNF install -y

  clean:
    - $DNF clean all

  aarch64: false

